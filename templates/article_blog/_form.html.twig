<div class="container">
{{ form_start(form) }}
    <div class="row">
         {{ form_row(form.Title) }} 
    </div>
    <div class="row"> {{ form_row(form.Content) }}</div>
    <div class="row"> {{ form_row(form.imageFile) }}</div>

    <button class="btn btn-brown">{{ button_label|default('Publier') }}</button>
{{ form_end(form) }}
</div>