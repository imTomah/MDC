{% extends 'base.html.twig' %}

{% block title %}Anti Gaspillage{% endblock %}

{% block body %}
    <h1>Les Astuces Anti Gaspillage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_blog.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article_blog.Title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article_blog.Content }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ article_blog.UpdatedAt ? article_blog.UpdatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <img src="{{ asset('images/{{ article_blog. }}') }}" alt="">

    <a href="{{ path('article_blog_index') }}">back to list</a>

    <a href="{{ path('article_blog_edit', {'id': article_blog.id}) }}">edit</a>

    {{ include('article_blog/_delete_form.html.twig') }}
{% endblock %}
